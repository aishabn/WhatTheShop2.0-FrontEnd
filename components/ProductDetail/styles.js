import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  border: {
    marginBottom: 10
  },
  title: {
    fontSize: 30,
    textAlign: "center",
    marginBottom: 30
  },
  description: {
    fontSize: 15,
    textAlign: "left",
    marginBottom: 15
  }
});

export default styles;
